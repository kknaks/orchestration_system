# Workflow

1. Read `kknaks_profile/.processed`.
2. Find unprocessed `.md` files in `kknaks_profile/tasks/planning/`.
3. Complete tasks in filename order.
4. Write reports to `kknaks_profile/reports/`.
5. Append processed task filenames to `kknaks_profile/.processed`.
